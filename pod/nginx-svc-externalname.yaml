apiVersion: v1
kind: Service
metadata:
  name: my-externalname-service
  labels:
    name: nginx
spec:
  selector:
    name: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ExternalName
  externalName: nginx.example.local