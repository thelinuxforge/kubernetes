apiVersion: apps/v1
kind: ReplicaSet
metadata:
  namespace: default
  name: nginx
  labels:
    name: nginx
spec:
  template:
    metadata:
      name: nginx
      labels: 
        name: backend
    spec:
      containers:
      - image: nginx
        name: nginx
  replicas: 1
  selector:
    matchLabels:
      name: backend
  
