apiVersion: v1
kind: Pod
metadata:
  name: "webapp"
  labels:
    name: "webapp"
spec:
  containers:
    - name: webapp
      image: ubuntu
      command: ["python", "app.py"] 
      args: ["--color", "green"]  
