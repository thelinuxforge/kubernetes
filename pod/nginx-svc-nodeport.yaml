apiVersion: v1
kind: Service
metadata:
  name: my-nodeport-service
  labels:
    name: nginx
spec:
  selector:
    name: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30007
  type: NodePort


